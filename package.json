{
  "name": "omarchy-pit-wall",
  "version": "1.0.0",
  "private": true,
  "description": "F1 pit wall widget for the Omarchy bar - dev harness for the Model.js data layer",
  "scripts": {
    "test": "node --test tests/*.test.js"
  }
}
